ANTHROPIC_API_KEY=
FIRECRAWL_API_KEY=

# Supabase (new key system, late 2025)
NEXT_PUBLIC_SUPABASE_URL=
# Browser-safe key (sb_publishable_...). Not used yet — reserved for any
# future client-side Supabase reads or auth.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Server-only key (sb_secret_...). Bypasses RLS — used by API routes
# and the audit pipeline. Legacy SUPABASE_SERVICE_ROLE_KEY is also accepted.
SUPABASE_SECRET_KEY=
